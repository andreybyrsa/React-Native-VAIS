import { StyleSheet } from 'react-native'

const PhoneConfirmPageStyles = StyleSheet.create({
  'phone-confirm-page__content': {
    display: 'flex',
    justifyContent: 'flex-start',
    alignItems: 'center',
    gap: 8
  },
})

export default PhoneConfirmPageStyles
